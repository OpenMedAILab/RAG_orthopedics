[project]
name = "rag-orthopedics"
version = "0.1.0"
description = "骨科 RAG 问答评估系统 - RAG vs No-RAG 医学问答质量对比评估"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "answer",
    "charts",
    "web",
]
